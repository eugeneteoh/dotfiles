name: ci

on:
  push:
  pull_request:

jobs:
  clone-and-install:
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/vscode/devcontainers/base:ubuntu
      env:
        CODESPACES: true
    steps:
      - uses: actions/checkout@v4
      - run: ./install.sh --promptBool isServer=true
      - name: chezmoi data
        run: '"$HOME/bin/chezmoi" data'
